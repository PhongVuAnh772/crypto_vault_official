import { StyleSheet } from 'react-native';

const appLogoLoadingAnimationStyle = StyleSheet.create({
    container: {
        zIndex: 9999,
    },
    icon: { width: 100, height: 100 },
});

export default appLogoLoadingAnimationStyle;
