import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
    itemContainer: {
        borderRadius: 4,
    },
    itemButton: {
        width: '31%',
    },
});

export default style;
