import { StyleSheet } from "react-native";

const CoinChartStyles = StyleSheet.create({
  container: {
    width: "100%",
    height: 200,
  },
  loaderContainer: {
    justifyContent: "center",
    alignItems: "center",
    height: 200,
  },
});

export default CoinChartStyles;
